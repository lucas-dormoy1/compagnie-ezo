---
globs: src/locale/messages.en.xlf
---

When translating content to English, never translate the titles of artistic pieces (creations, performances, projects). These are proper names and must remain in French, especially when they appear in italic (`<i>` tags / `START_ITALIC_TEXT` / `CLOSE_ITALIC_TEXT` markers in XLF).
